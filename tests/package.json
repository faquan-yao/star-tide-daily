{
  "name": "star-tide-daily-tests",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test pipeline-agent.test.mjs && node --test run-pipeline-step.test.mjs && node validate-output.mjs --all",
    "test:static": "./preflight.sh --static",
    "test:preflight": "./preflight.sh",
    "test:unit": "node --test pipeline-agent.test.mjs && node --test run-pipeline-step.test.mjs",
    "test:fixtures": "node validate-output.mjs --all"
  }
}
